import * as React from 'react';
import DisplayContainer from './display_container';

// tslint:disable-next-line
export const Body = () => {
  return (
    <div className="body">
      <DisplayContainer/>
    </div>
  );
};
