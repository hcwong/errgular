package testhelpers

// TODO: Make these queries the same as the one in queries.go to ensure consistency?
// const tqCreateErrorsTable = "create table if not exists errors (id serial primary key, project_name varchar(255) not null, error_code int not null)"

// TestQueries to load test db data
var TestQueries = []string{}
